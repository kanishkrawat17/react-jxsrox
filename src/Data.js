import React from 'react';

const Data = (props)=>{
  console.log(props.data)
  return <>ok</>
    // {props.data.length == 0 ? 
    //   <></>
    //   : data.map((el)=>{
    //     return <p>{el.name}</p>
    //   })}
  
}

export default Data